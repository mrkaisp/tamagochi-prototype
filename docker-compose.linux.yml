version: "3.9"
services:
  app:
    build: .
    container_name: tamagotchi-pygame
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - .:/app
      - /tmp/.X11-unix:/tmp/.X11-unix
    working_dir: /app
    stdin_open: true
    tty: true
    command: python -m src.main
